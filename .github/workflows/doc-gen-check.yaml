---
name: Check doc-gen

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: python3 update-generated.py install-readme
      - run: git diff --exit-code
