# vi: ft=conf

# keep an eye on this
# https://github.com/ghostty-org/ghostty/discussions/3501
font-family                   = Fira Code
font-style                    = Light
font-size                     = 18

font-feature                  = calt
font-feature                  = zero
font-feature                  = ss05
font-feature                  = ss03

theme                         = Kanagawa Wave

adjust-cell-height            = 5%

cursor-style                  = block
cursor-style-blink            = false
shell-integration-features    = no-cursor

window-width                  = 1000
window-height                 = 1000
window-padding-x              = 16
window-padding-y              = 16

quit-after-last-window-closed = true

macos-option-as-alt           = left

# this can be removed after a fix is merged
# https://github.com/ghostty-org/ghostty/issues/3646
# Will be fixed in v1.0.1
keybind                       = cmd+backspace=text:\x15
