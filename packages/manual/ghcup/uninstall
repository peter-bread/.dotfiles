#!/usr/bin/env bash

# Uninstall GHCup:
# - Uninstalls everything

set -Eeuo pipefail

source "$DOTFILES"/_helpers/core

GHCUP=$HOME/.ghcup/bin/ghcup

info "Uninstalling GHCup"

need "$GHCUP"

"$GHCUP" nuke

success "GHCup uninstalled"
